[build]
  command = "npm run build"
  publish = "dist"

# Contact form uses Web3Forms. In Netlify: Site settings → Environment variables →
# add VITE_WEB3FORMS_ACCESS_KEY (same value as in local .env), then redeploy.
